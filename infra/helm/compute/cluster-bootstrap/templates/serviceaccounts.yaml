apiVersion: v1
kind: ServiceAccount
metadata:
  name: spark
  namespace: spark-jobs
  annotations:
    azure.workload.identity/client-id: {{ .Values.workloadIdentity.spark.clientId | quote }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: trino
  namespace: trino
  annotations:
    azure.workload.identity/client-id: {{ .Values.workloadIdentity.trino.clientId | quote }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hive-metastore
  namespace: hive-metastore
  annotations:
    azure.workload.identity/client-id: {{ .Values.workloadIdentity.hms.clientId | quote }}
