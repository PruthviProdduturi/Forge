apiVersion: v1
kind: Service
metadata:
  name: portal-auth-proxy
  namespace: {{ .Release.Namespace }}
  labels:
    app: portal-auth-proxy
spec:
  selector:
    app: portal-auth-proxy
  ports:
    - name: http
      port: 8080
      targetPort: 8080
