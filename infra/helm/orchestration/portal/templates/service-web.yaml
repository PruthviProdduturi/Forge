apiVersion: v1
kind: Service
metadata:
  name: portal-web
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: portal-web
  ports:
    - name: http
      port: 3001
      targetPort: 3001
