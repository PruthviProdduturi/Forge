# =============================================================================
# Forge — Airflow Image Python Requirements
# Pinned versions for the forgeacr/airflow:3.1.8 image.
#
# These packages are installed on top of apache/airflow:3.1.8-python3.11.
# Provider packages are installed with Airflow's official constraints file
# (see Dockerfile). Non-provider packages are listed here for reference and
# are installed directly.
#
# Last reviewed: 2026-03-24
# =============================================================================

# =============================================================================
# Airflow Providers
# Install with: pip install -c constraints-3.11.txt <package>==<version>
# =============================================================================

# Kubernetes provider — SparkKubernetesOperator, KubernetesPodOperator
# Version determined by the Airflow constraints file (constraints-3.1.8/constraints-3.11.txt)
apache-airflow-providers-cncf-kubernetes

# Azure provider — ADLS hooks, Azure Key Vault secrets backend
# Version determined by the Airflow constraints file
apache-airflow-providers-microsoft-azure

# OpenLineage provider — wraps the openlineage-airflow package into an
# Airflow-native integration with DAG-level configuration support
# Version determined by the Airflow constraints file
apache-airflow-providers-openlineage

# =============================================================================
# OpenLineage
# =============================================================================

# OpenLineage is provided via apache-airflow-providers-openlineage (above).
# openlineage-airflow is the legacy package superseded by the official provider
# in Airflow 3.x — do NOT install it separately as it conflicts with the provider.

# =============================================================================
# Azure SDK
# =============================================================================

# Azure Workload Identity credential provider
# Min 1.24.0 required by azure-kusto-data in the Airflow 3.1.8 base image
azure-identity>=1.24.0

# Azure Data Lake Storage Gen2 (ADLS) file operations
azure-storage-file-datalake>=12.18.0

# Azure Key Vault secrets (used by the Key Vault secrets backend)
azure-keyvault-secrets>=4.8.0

# Azure Core (transitive dependency)
azure-core>=1.31.0

# Security: pin cryptography to fix GHSA-r6ph-v2qm-q3c2 (S360 VulnId 5007602)
cryptography==46.0.5

# =============================================================================
# Data processing
# =============================================================================

# Apache Arrow and Pandas are already present in the Airflow 3.1.8 base image
# at versions newer than what we need — no need to pin here.

# =============================================================================
# HTTP / networking
# =============================================================================

# Requests — HTTP client used by custom Airflow hooks and operators
requests==2.32.3

# Tenacity — retry library used by Forge operators and DQ SDK
tenacity==9.0.0

# =============================================================================
# Serialisation / validation
# =============================================================================

# Pydantic v2 — used by Forge DQ SDK and lineage SDK for model validation
pydantic==2.7.4

# =============================================================================
# Database (Airflow metadata DB migrations run via Airflow, not this code)
# =============================================================================

# Psycopg2 — PostgreSQL adapter for Airflow metadata DB connection
# (built from source — requires libpq-dev in the Dockerfile)
psycopg2==2.9.9

# =============================================================================
# Utilities
# =============================================================================

# Python-dotenv — read .env files in local development DAG testing
python-dotenv==1.0.1

# Croniter — cron expression parsing used by SLA check operators
croniter==2.0.5

# PyYAML — YAML config loading used by DQ rulesets and lineage configs
PyYAML==6.0.2

# =============================================================================
# Forge DQ SDK
# =============================================================================

# forge-dq — installed from local source in Dockerfile, listed here for reference
# forge-dq==0.1.0  (installed via COPY + pip install in Dockerfile, not from PyPI)

# Purview SDK — lineage catalog integration
azure-purview-catalog==1.0.0b7
azure-purview-scanning==1.0.0b2
