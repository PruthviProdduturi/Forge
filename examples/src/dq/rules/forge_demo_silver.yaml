# DQ rules for forge_demo_silver — edit this file to add domain rules
# Generated by: forge generate --job forge_demo_silver
# Applied by:   forge_dq @track decorator inside forge_demo_silver.py
#
# Severity levels:
#   critical — job fails on violation (fail_fast=true)
#   warning  — logged and continued; never fails the job
#
# Rule types supported by forge_dq:
#   not_null, row_count, unique, accepted_values,
#   min_value, max_value, regex, custom_sql, max_staleness_hours

version: "v1"
dataset: "lakehouse.silver.retail_orders_cleaned"
description: "Quality rules for forge_demo_silver"
owner: data-engineering
tags: ["silver", "forge_demo_silver", "forge-demo", "retail", "daily"]

rules:

  # ---------------------------------------------------------------------------
  # Completeness — partition columns
  # ---------------------------------------------------------------------------

  - name: not_null___date
    type: not_null
    column: __date
    severity: critical
    description: Partition column must never be null.

  # ---------------------------------------------------------------------------
  # Uniqueness
  # ---------------------------------------------------------------------------
  - name: row_count_positive
    type: row_count
    min: 1
    severity: critical
    description: Table must contain at least one row per partition.

  # ---------------------------------------------------------------------------
  # Freshness (add threshold appropriate for your schedule)
  # ---------------------------------------------------------------------------
  # - name: freshness_check
  #   type: max_staleness_hours
  #   column: _processed_at
  #   max_hours: 25
  #   severity: warning

  # ---------------------------------------------------------------------------
  # TODO: add domain-specific rules below
  # ---------------------------------------------------------------------------
  # Example:
  # - name: status_accepted_values
  #   type: accepted_values
  #   column: status
  #   values: [pending, confirmed, shipped, delivered, cancelled]
  #   severity: warning
